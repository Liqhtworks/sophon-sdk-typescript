# DownloadsApi

All URIs are relative to *https://api.liqhtworks.xyz*

| Method | HTTP request | Description |
|------------- | ------------- | -------------|
| [**download**](DownloadsApi.md#download) | **GET** /v1/downloads/{token} | Download an output file via signed token |



## download

> Blob download(token)

Download an output file via signed token

Serves the encoded output file. No Authorization header required -- the HMAC-signed token (generated by GET /v1/jobs/{id}/output) IS the credential. Tokens expire after 24 hours. 

### Example

```ts
import {
  Configuration,
  DownloadsApi,
} from '@liqhtworks/sophon-sdk';
import type { DownloadRequest } from '@liqhtworks/sophon-sdk';

async function example() {
  console.log("🚀 Testing @liqhtworks/sophon-sdk SDK...");
  const api = new DownloadsApi();

  const body = {
    // string | HMAC-signed download token encoding the object key and expiry.
    token: token_example,
  } satisfies DownloadRequest;

  try {
    const data = await api.download(body);
    console.log(data);
  } catch (error) {
    console.error(error);
  }
}

// Run the test
example().catch(console.error);
```

### Parameters


| Name | Type | Description  | Notes |
|------------- | ------------- | ------------- | -------------|
| **token** | `string` | HMAC-signed download token encoding the object key and expiry. | [Defaults to `undefined`] |

### Return type

**Blob**

### Authorization

No authorization required

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: `video/mp4`, `application/json`


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
| **200** | File stream. |  * Content-Disposition - attachment; filename&#x3D;\&quot;&lt;output-filename&gt;\&quot; <br>  * Content-Type - video/mp4 <br>  * Content-Length -  <br>  * X-Request-Id -  <br>  |
| **403** | Invalid or expired download token. |  * X-Request-Id -  <br>  |
| **404** | Output file not found on disk. |  * X-Request-Id -  <br>  |

[[Back to top]](#) [[Back to API list]](../README.md#api-endpoints) [[Back to Model list]](../README.md#models) [[Back to README]](../README.md)

